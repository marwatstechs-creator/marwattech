import { NotFoundCosmic } from "@/components/marketing/not-found-cosmic";

/**
 * Marketing 404 — renders inside the marketing layout so the navbar, footer
 * and WhatsApp button stay visible on not-found pages.
 */
export default function MarketingNotFound() {
  return <NotFoundCosmic />;
}
